{
  "name": "eggdone",
  "version": "1.2.1",
  "description": "EggTodo - 轻量级托盘 Todo 桌面应用",
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "build:windows": "tauri build --bundles nsis",
    "i18n:check": "node scripts/check-i18n-catalogs.mjs && node scripts/check-i18n-hardcodes.mjs",
    "release:check": "pnpm i18n:check && pnpm check && pnpm test && pnpm build && cd src-tauri && cargo fmt -- --check && cargo check && cargo test",
    "test": "vitest run",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "tauri": "tauri"
  },
  "license": "MIT",
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-autostart": "^2.5.1",
    "@tauri-apps/plugin-global-shortcut": "^2.3.2"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.9.0",
    "@sveltejs/vite-plugin-svelte": "^5.1.1",
    "@tauri-apps/cli": "^2",
    "@types/node": "^25.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
