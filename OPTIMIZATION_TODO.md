# EggDone 优化 TODO

本文档用于跟踪 EggDone 从 MVP 到可稳定发布版本的优化工作。任务按依赖关系排列；除紧急缺陷外，不跨阶段开发。

## 目标

- 保持托盘 Todo 的轻量定位。
- 提升数据可靠性、任务操作效率和原生桌面体验。
- 支持 S3、MinIO 等 S3 兼容存储的跨设备同步。
- 建立可测试、可迁移、可发布的工程基础。

## P0：数据与运行基础

后续任务编辑、排序和同步都依赖本阶段。

### 数据库迁移

- [x] 建立 `schema_migrations` 表和顺序 migration 机制。
- [x] 为现有 Todo 生成全局唯一 `uuid`。
- [x] 新增 `completed_at`、`deleted_at`、`sort_order` 字段。
- [x] 将时间字段统一为 UTC 毫秒时间戳。
- [x] 保证旧数据库升级后数据不丢失。
- [x] 为 migration 增加全新数据库和旧数据库升级测试。

验收标准：

- 旧版 `eggdone.sqlite3` 可直接升级。
- 重复执行 migration 不会报错或重复修改数据。
- 新增、完成、排序和软删除字段均有明确默认值。

### 单实例运行

- [x] 接入 Tauri 单实例插件。
- [x] 重复启动时激活已有进程并显示 Todo 面板。
- [x] 将第二个进程收到的参数转发给主进程。

验收标准：

- 任意时刻只有一个 EggDone 进程。
- 再次启动不会创建第二个托盘图标。

### 数据备份与交换

- [x] 增加 JSON 导出。
- [x] 增加 JSON 导入预览和确认。
- [x] 导入时按 UUID 合并，不直接覆盖数据库。
- [x] 增加手动 SQLite 备份。
- [x] 导入、导出格式包含 `format_version`。

验收标准：

- 导出后可以在全新数据库中恢复全部任务。
- 无效或高版本 JSON 不会破坏本地数据。

## P1：Todo 核心体验

### 任务编辑

- [x] 双击任务进入行内编辑。
- [x] Enter 保存，Escape 取消。
- [x] 空标题禁止保存。
- [x] 保存后更新 `updated_at`。
- [x] 编辑失败时保留用户输入并显示错误。

### 任务管理

- [x] 增加“清除已完成”操作和二次确认。
- [x] 完成任务时写入 `completed_at`，取消完成时清空。
- [x] 支持拖动排序并持久化 `sort_order`。
- [x] 删除改为软删除，为同步保留删除记录。
- [x] 提供 5 秒删除撤销。

### 交互反馈

- [x] 新增任务使用轻量进入动画。
- [x] 完成状态使用 checkbox 和文字过渡动画。
- [x] 删除使用退出动画，完成后显示撤销提示。
- [x] 尊重系统 `prefers-reduced-motion` 设置。
- [x] 所有动画不阻塞数据库操作。

验收标准：

- 键盘可以完成新增、编辑、完成和取消编辑。
- 排序、完成时间和删除状态在重启后保持。
- 快速连续操作不会产生重复任务或状态错乱。

## P2：原生桌面体验

### 托盘与窗口

- [x] 按托盘所在显示器计算面板位置。
- [ ] 验证 Windows 100%、125%、150%、200% 缩放。
- [x] 面板不得超出当前显示器工作区。
- [x] 优化点击托盘时失焦隐藏与重新显示的竞态。
- [x] 避免点击面板内部菜单或弹窗时意外隐藏。
- [x] 在 macOS 和 Linux 上保留合理定位回退。

### 快捷操作

- [x] 增加全局快捷键，默认 `Ctrl + Shift + Space`。
- [x] 快捷键用于切换面板显隐并聚焦输入框。
- [x] 支持用户修改或禁用快捷键。
- [x] 检测快捷键冲突并显示错误。

### 开机启动

- [x] 增加开机启动设置。
- [x] 开机启动时静默进入托盘。
- [x] 手动启动和开机启动使用相同单实例逻辑。
- [x] 明确显示当前系统设置状态。

验收标准：

- 多显示器和高 DPI 环境下，面板始终出现在可见区域。
- 开机启动不会弹出普通窗口。
- 快捷键在面板隐藏和显示状态下都工作正常。

## P3：S3 / MinIO 同步

第一版只实现手动同步。自动同步在冲突和数据安全验证通过后再启用。

### 同步数据模型

- [x] 定义版本化 `todos.json` 格式。
- [x] 同步内容包含 Todo UUID、时间、排序和软删除状态。
- [x] 生成并持久化本机 `device_id`。
- [x] 按 UUID 合并本地和远端任务。
- [x] 冲突默认采用较新 `updated_at`，相同时间按稳定规则决胜。
- [x] 为合并、删除和排序冲突编写单元测试。

建议对象结构：

```text
eggdone/
└─ users/
   └─ <sync-id>/
      └─ todos.json
```

### S3 / MinIO 配置

- [x] 支持自定义 Endpoint。
- [x] 支持 AWS S3、MinIO 和其他 S3 兼容服务。
- [x] 支持 HTTP 和 HTTPS Endpoint。
- [x] HTTP 配置显示明文传输警告，但允许用户继续。
- [x] 支持 Region、Bucket、Object Key 和 Path Style。
- [x] 增加“测试连接”。
- [x] 增加“立即同步”。

### 凭据安全

- [x] Access Key 和 Secret Key 保存到系统凭据库。
- [x] SQLite 只保存非敏感同步配置。
- [x] 日志和错误信息不得包含 Secret Key。
- [x] 提供删除凭据和禁用同步操作。

### 冲突保护

- [x] 下载对象时记录 ETag。
- [x] 上传时使用 `If-Match`。
- [x] 首次创建使用 `If-None-Match: *`。
- [x] ETag 冲突后重新下载、合并并重试一次。
- [x] 重试仍失败时停止写入并保留本地数据。
- [x] 显示同步中、已同步、离线、冲突和失败状态。

### 自动同步

- [x] 启动后拉取一次。
- [x] 本地修改后延迟 3 至 5 秒同步。
- [x] 同一时间只允许一个同步任务运行。
- [x] 网络失败使用有限次数指数退避。
- [x] 应用退出前不强制等待长期网络请求。

验收标准：

- 两台设备可新增、编辑、完成和删除任务，合并后不丢数据。
- 并发修改不会无条件覆盖远端新版本。
- MinIO HTTP、MinIO HTTPS 和至少一种标准 S3 服务通过验证。
- 断网和凭据错误不会影响本地 Todo 使用。

## P4：测试、安装与发布

### 自动化测试

- [x] Todo store：新增、编辑、完成、删除和错误状态测试。
- [x] SQLite：migration 和 CRUD 集成测试。
- [x] 同步：合并、ETag 冲突和软删除测试。
- [x] 托盘显隐状态逻辑测试。
- [x] JSON 导入导出往返测试。
- [x] 为关键用户流程维护手动回归清单。

### Windows 发布

- [x] 完善产品名称、公司信息、版本和版权信息。
- [x] 生成 NSIS 或 MSI 安装包。
- [x] 验证安装、升级、降级拦截和卸载。
- [ ] 配置 Windows 代码签名。
- [ ] 增加自动更新及签名验证。
- [x] 编写发布说明和回滚步骤。

### 跨平台验证

- [ ] macOS 菜单栏、窗口定位和快捷键验证。
- [ ] Linux AppIndicator、窗口定位和凭据库验证。
- [ ] 明确各平台不支持或降级的功能。

验收标准：

- 安装包可完成安装、升级和卸载，用户数据保持不变。
- 自动更新失败时旧版本仍可运行。
- 发布构建通过类型检查、Rust 检查和完整构建。

## 推荐版本划分

### v0.2：可靠数据与任务体验

- 数据库 migration
- UUID、完成时间、软删除和排序
- 单实例运行
- 任务编辑、清除已完成、拖动排序和删除撤销
- JSON 导入导出与备份

### v0.3：桌面集成

- 多显示器和高 DPI 优化
- 全局快捷键
- 开机启动
- 关键逻辑自动化测试

### v0.4：手动同步

- S3 / MinIO 配置
- 系统凭据库
- 手动同步
- UUID 合并
- ETag 冲突保护
- 同步状态反馈

### v0.5：自动同步与发布

- 启动同步和延迟自动同步
- 网络重试
- Windows 安装包、签名和自动更新
- macOS、Linux 实机验证

## 暂不计划

- 用户账号和自建业务服务端
- 实时 WebSocket 同步
- 团队共享任务
- 复杂分类、项目和标签系统
- 直接同步 SQLite 数据库文件
- 应用内自动执行 Git pull、commit 和 push
